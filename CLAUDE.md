# ERP System — 프로젝트 지침

## 1. 프로젝트 개요
- 목표: 이카운트(https://www.ecount.com/kr/)를 벤치마크한 웹 기반 멀티테넌트 ERP SaaS.
- 대상: 한국 중소기업. 한 회사(테넌트)가 가입하면 사용자 무제한으로 전 모듈 사용.
- 언어: UI/데이터는 한국어 우선, i18n 구조는 처음부터 준비(ko 기본, en 확장 가능).

## 2. 기술 스택 (고정 — 임의 변경 금지)
- 프론트: Next.js (App Router) + TypeScript + Tailwind CSS + shadcn/ui
- 백엔드: Supabase (PostgreSQL, Auth, RLS, Storage, Edge Functions)
- 상태/데이터: TanStack Query + Server Actions. 전역 상태 라이브러리는 꼭 필요할 때만.
- 테스트: Vitest(단위) + Playwright(E2E)
- 배포: Vercel + Supabase 프로젝트

## 3. 멀티테넌시 원칙 (최우선 불변 규칙)
- 모든 업무 테이블은 `company_id uuid not null` 컬럼을 갖는다.
- 테넌트 격리는 Supabase RLS로 강제한다. 애플리케이션 코드의 where 절만으로 격리하지 않는다.
- RLS 정책 패턴: `company_id = (auth.jwt() ->> 'company_id')::uuid` 또는 memberships 조회 기반.
- 신규 테이블 생성 시 RLS 활성화 + 정책 작성이 없으면 그 마이그레이션은 미완성이다.
- 역할: owner / admin / manager / member. 모듈별 권한은 permissions 테이블로 관리.

## 4. 모듈 범위 (구현 순서 = docs/plan.md의 Phase 순서)
1. 기반: 인증, 회사 가입, 멤버 초대, 권한, 공통 UI 셸
2. 기준정보: 품목, 거래처, 창고, 계정과목, 부서/사원
3. 재고/유통: 입출고, 재고수불부, 창고별 재고, 재고조정, 안전재고 알림
4. 영업/판매: 견적 → 주문 → 판매(출고) → 거래명세서, 미수금 관리
5. 구매/발주: 발주 → 구매(입고), 미지급금 관리
6. 경리/회계: 전표(자동분개 포함), 매입매출장, 채권채무, 재무제표(합계잔액시산표, 손익계산서, 재무상태표)
7. 생산/제조: BOM, 작업지시, 생산입고(자재 차감), 공정 관리
8. 인사/급여: 사원 관리, 근태, 급여대장(4대보험/소득세 간이 계산), 급여명세서
9. 그룹웨어: 전자결재(판매/구매/지출 문서 연동), 게시판, 일정
- 이후 외부 연동: 전자세금계산서(10), 홈택스(11), POS(12), 쇼핑몰(13)
- 각 모듈의 화면·필드 구성이 애매하면 이카운트의 해당 메뉴 구조를 기준으로 삼는다.

## 5. 핵심 도메인 규칙
- 재고 수량은 직접 수정하지 않는다. 모든 변동은 stock_movements(수불 이력)를 통해서만 발생하고, 현재고는 이력의 합계 또는 트리거 유지 캐시로 계산한다.
- 판매/구매/생산 전표는 승인(확정) 시점에만 재고와 회계에 반영한다. 임시저장 상태는 반영하지 않는다.
- 회계 전표는 차변 합계 = 대변 합계 검증을 DB 제약 또는 트랜잭션 검증으로 강제한다.
- 확정된 전표의 수정은 금지하고, 취소 전표(역분개) + 신규 전표로 처리한다.
- 금액은 numeric(18,2), 수량은 numeric(18,4). float 사용 금지.
- 문서번호는 테넌트별 연속 채번(예: SO-2026-0001). 채번 로직은 동시성 안전하게 DB 함수로 구현한다.

## 6. 행동 지침 (Fable 5)
- 행동할 만큼 정보가 모이면 즉시 행동한다. 이미 확정된 결정을 다시 논의하거나, 선택하지 않을 옵션을 나열하지 않는다. 선택지를 저울질 중이면 전수 조사가 아니라 추천안 하나를 제시한다.
- 작업이 요구하는 것 이상의 기능·리팩토링·추상화를 추가하지 않는다. 가상의 미래 요구사항을 위해 설계하지 않는다. 발생할 수 없는 시나리오에 대한 에러 처리를 넣지 않는다. 검증은 시스템 경계(사용자 입력, 외부 API)에서만 한다.
- 진행 상황을 보고하기 전에, 각 주장을 이 세션의 실제 도구 실행 결과와 대조한다. 증거를 가리킬 수 있는 작업만 완료로 보고한다. 테스트가 실패하면 출력과 함께 실패했다고 말한다. 건너뛴 단계는 건너뛰었다고 말한다.
- 사용자에게 멈춰서 묻는 경우는 세 가지뿐이다. 파괴적/비가역적 작업, 실질적인 범위 변경, 사용자만 줄 수 있는 입력. 그 외에는 계속 진행한다.
- 사용자가 문제를 설명하거나 질문하는 중이면 결과물은 나의 분석이다. 진단만 보고하고 멈춘다. 수정 요청이 오기 전에 고치지 않는다.
- 독립적인 하위 작업은 서브에이전트에 위임하고 그동안 계속 작업한다. 큰 Phase 완료 시 별도 컨텍스트의 검증 서브에이전트로 명세 대비 검증한다.

## 7. 코딩 규칙
- 단순함이 최우선. 문제를 푸는 최소한의 코드만 작성한다. 200줄이 50줄로 줄 수 있으면 다시 쓴다.
- 수술적 변경. 요청과 무관한 코드·주석·서식을 건드리지 않는다. 변경된 모든 줄은 요청과 직접 연결되어야 한다.
- 모든 새 소스 파일 첫 줄에 파일 역할을 설명하는 한 줄 한국어 주석을 단다('use client' 등 지시문 바로 아래). 설정 파일은 제외.
  예: `// 판매 전표 확정 시 재고 차감과 회계 분개를 처리하는 Server Action`
- 에러가 나면 전체 에러 메시지와 스택 트레이스를 읽고 원인을 확인한 뒤 고친다. 패턴 매칭으로 추측 수정하지 않는다.
- 한국어 출력에서 문장 끝에 콜론을 쓰지 않는다. 문장은 마침표로 끝낸다.

## 8. 작업 관리 산출물
- `docs/plan.md` — 전체 계획. 무엇을 왜 만드는지.
- `docs/checklist.md` — Phase별 구체 작업 목록. 완료 시 체크.
- `docs/context-notes.md` — 작업 중 내린 결정과 근거. 지속적으로 추가.
- `docs/memory/` — 세션 간 교훈 저장소. 파일당 교훈 1개, 첫 줄에 한 줄 요약. 확인된 접근법과 교정 사항을 이유와 함께 기록. 저장소나 대화 이력이 이미 기록한 것은 저장하지 않고, 중복 생성 대신 기존 노트를 갱신하며, 틀린 것으로 판명된 노트는 삭제한다.
- 새 세션 시작 시 위 4개를 먼저 읽고 이어서 작업한다.

## 9. 완료 기준 (Definition of Done)
- `npm run build` 성공 + `npx tsc --noEmit` 통과 + 관련 테스트 통과. 이 세 가지 확인 전에 "완료"라고 말하지 않는다.
- 신규 테이블: 마이그레이션 + RLS 정책 + 시드 데이터 포함.
- 신규 화면: 로딩/빈 상태/에러 상태 처리 포함.
- 논리적 변경 단위마다 의미 있는 한 문장으로 설명 가능한 커밋을 즉시 만든다.
