# 이 저장소는 공개(public)다. 계정 정보가 들어가는 파일은 올리지 않는다.
# renderer/index.html 에는 AUTO_LOGIN(관리자 이메일·비밀번호)이 평문으로 들어 있다.
renderer/index.html
# build-renderer.js 가 읽는 자동 로그인 계정
desktop.secrets.json
build.log

# CDN 라이브러리 번들 — 용량만 차지하고 언제든 다시 받을 수 있다
renderer/vendor/

# 빌드 산출물
node_modules/
dist/
